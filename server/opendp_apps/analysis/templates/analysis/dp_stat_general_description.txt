A differentially private {{ stat.statistic|title }} for variable "{{ stat.variable}}" was calculated with the result{{ stat.value}}. {% if stat.accuracy_val %}There is a probability of {{ stat.get_ci_text }} that the output count will differ from the true count by at most {{ stat.accuracy_val}} units. Here the units are the same units the variable has in the dataset.{% endif %}